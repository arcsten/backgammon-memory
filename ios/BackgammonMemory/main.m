#import <UIKit/UIKit.h>

#import "AppDelegate.h"
// Frame processors disabled; no imports required

int main(int argc, char * argv[]) {
  @autoreleasepool {
    // Frame processor temporarily disabled due to build issues with react-native-worklets-core
    return UIApplicationMain(argc, argv, nil, NSStringFromClass([AppDelegate class]));
  }
}

